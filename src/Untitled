:root {
  font-family: Inter, system-ui, Avenir, Helvetica, Arial, sans-serif;
  line-height: 1.5;
  font-weight: 400;
  
  /* Quy định màu nền mặc định */
  background-color: #0f0f11; 
  color: #e4e4e7;
  
  /* Ngăn người dùng bôi đen văn bản (cảm giác giống App native) */
  user-select: none;
  /* Ẩn con trỏ mặc định, dùng con trỏ hệ thống */
  cursor: default;
}

body {
  margin: 0;
  display: flex;
  place-items: center;
  min-width: 320px;
  min-height: 100vh;
  overflow: hidden; /* Ẩn thanh cuộn */
}

/* Hiệu ứng Glassmorphism dùng chung */
.glass-panel {
  @apply bg-surface/80 backdrop-blur-md border border-white/5 rounded-2xl shadow-xl;
}